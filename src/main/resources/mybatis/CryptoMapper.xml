<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wal.hs.crypto.dao.mapper.CryptoMapper">
	<cache />


	<insert id="insertAcc" parameterType="String">
		INSERT INTO crypto
		(account)
		VALUES (#{accountNo})
	</insert>

</mapper>